// Empty module — used by metro.config.js to stub Node-only imports that some
// dependencies reference in type-only positions (e.g. react-native-svg's unused
// SVG WebView component imports a `buffer` type). Never executed at runtime.
module.exports = {};

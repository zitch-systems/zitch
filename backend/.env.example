# Copy to .env for local development (never commit the real .env).

# Core
DJANGO_SECRET_KEY=dev-insecure-change-me
DJANGO_DEBUG=true
DJANGO_ALLOWED_HOSTS=*
CSRF_TRUSTED_ORIGINS=http://localhost:8081,http://localhost:19006

# Database — leave unset to use local SQLite. On Render this is injected.
# DATABASE_URL=postgres://USER:PASS@HOST:5432/DBNAME

# Session lifetime for issued access tokens, in hours.
TOKEN_TTL_HOURS=24

# --- Third-party services (leave blank in dev: the app runs in MOCK mode) ---
# VTU / bills aggregator (airtime, data, cable, electricity)
VTPASS_BASE_URL=https://sandbox.vtpass.com/api
VTPASS_API_KEY=
VTPASS_SECRET_KEY=

# Payment gateway (wallet funding)
PAYSTACK_SECRET_KEY=
PAYSTACK_PUBLIC_KEY=

# SMS / OTP
TERMII_API_KEY=
TERMII_SENDER_ID=Zitch

# KYC — BVN / NIN / liveness (Dojah example)
KYC_BASE_URL=https://api.dojah.io
KYC_APP_ID=
KYC_SECRET_KEY=

# Card issuer — virtual cards (Sudo Africa example)
CARD_ISSUER_BASE_URL=https://api.sudo.africa
CARD_ISSUER_API_KEY=
CARD_ISSUER_BRAND=Verve
